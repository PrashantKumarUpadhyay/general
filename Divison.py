import math
import os
import random
import re
import sys

a=input("what is number a:")
b=input("what is number b:")

def intdiv(a,b):
    x=int(a)
    y=int(b)
    z=x//y
    print(z)
def fltdiv(a,b):
    x=float(a)
    y=float(b)
    z=x/y
    print(z)
intdiv(a,b)
fltdiv(a,b)